apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "su-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "su-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: []
  restartPolicy: Never
